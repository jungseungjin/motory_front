import React from 'react';
import styled from 'styled-components/native';

const Auth = styled.TouchableOpacity`
  height: 50px;
  width: 20%;
  margin-left: 5%;
  justify-content: center;
  background-color: #ffffff;
  color: rgba(0, 0, 0, 0.2);
  border: 1px solid rgba(0, 0, 0, 0.2);
  border-radius: 5px;
  margin-top: 5%;
`;

export default Auth;
